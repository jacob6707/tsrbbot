const { Message, Client } = require("discord.js");
/**
 * 
 * @param {Client} client
 * @param {Message} message 
 * @param {Message} answer 
 * @param {string} query
 */
module.exports = (client, message, answer, query) => {
    
};